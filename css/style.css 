/* RESET */
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Inter', sans-serif;
    background: #1b2228; /* dark bluish grey like screenshot */
    color: #ffffff;
}

/* TOP NAVBAR */
.topbar {
    position: fixed;
    top: 0;
    width: 100%;
    height: 70px;
    background: #1b2228;
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 0 40px;
    z-index: 1000;
}

/* LEFT PROFILE */
.profile-circle {
    width: 40px;
    height: 40px;
    background: #2a3138;
    border-radius: 50%;
}

/* NAV CENTER */
.nav {
    display: flex;
    gap: 20px;
}

.nav-item {
    text-decoration: none;
    color: #bfc6cc;
    font-weight: 500;
    padding: 10px 22px;
    border-radius: 999px;
    transition: all 0.3s ease;
}

.nav-item:hover {
    color: white;
}

.nav-item.active {
    background: #11161b; /* deep pill highlight */
    color: white;
}

/* RIGHT ICONS */
.right {
    display: flex;
    gap: 15px;
}

.icon-btn {
    width: 36px;
    height: 36px;
    background: #2a3138;
    border-radius: 999px;
}

/* MAIN CONTENT */
.content {
    padding: 120px 40px 40px 40px;
}

/* SECTIONS */
.section {
    margin-bottom: 50px;
}

.section h2 {
    margin-bottom: 20px;
    font-size: 22px;
    font-weight: 600;
    color: #e6e6e6;
}

/* CARD ROW */
.card-row {
    display: flex;
    gap: 20px;
    overflow-x: auto;
    padding-bottom: 10px;
}

/* CARDS */
.card {
    background: #2a3138;
    border-radius: 18px;
    flex-shrink: 0;
    transition: transform 0.3s ease;
}

.card:hover {
    transform: scale(1.05);
}

/* WIDE CARDS (Current Watching) */
.card.wide {
    width: 320px;
    height: 180px;
}

/* TALL CARDS (Movies) */
.card.tall {
    width: 160px;
    height: 240px;
}